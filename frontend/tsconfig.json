{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "skipLibCheck": true,
    // Asserted here so a generated-config change can't silently drop it.
    "strict": true
  }
}
