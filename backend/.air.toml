root = "."
tmp_dir = "bin/air"

[build]
  cmd = "go build -o bin/air/lifer ./cmd/server"
  bin = "bin/air/lifer"
  include_ext = ["go"]
  exclude_dir = ["bin", "migrations"]
  delay = 500

[log]
  time = false
